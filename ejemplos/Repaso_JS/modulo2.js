const cadena = "prueba cadena";
const sumar = (a, b) => a + b;
const restar = function (a, b) {
  return a - b;
};
const mul = function (a, b) {
  return a * b;
};

export default mul;
export { cadena, sumar, restar };
